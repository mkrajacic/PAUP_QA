@using QA.Models
@using QA.Misc
@model KategorijaAzuriranje
@{
    ViewBag.Title = "Ažuriraj kategoriju";
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <link href="https://fonts.googleapis.com/css2?family=Noto+Sans:ital,wght@0,400;0,700;1,400&display=swap" rel="stylesheet">
    <title>@ViewBag.Title</title>
</head>
<body>
    <header>
        <nav>
            <ul>
                <li>@Html.ActionLink("Natrag na pitanja", "Index", "App")</li>
            </ul>
        </nav>
    </header>
    <section class="body">
        <section class="form-section">
            <h1>@ViewBag.Title</h1>
            @using (Html.BeginForm("AzurirajKategoriju", "App", FormMethod.Post, new { @class = "login-form" }))
            {
                @Html.AntiForgeryToken()
                @Html.ValidationMessageFor(m => m.Kategorija, "", new { @style = "color:red;" })
                @Html.LabelFor(model => model.Kategorija)
                @Html.EditorFor(model => model.Kategorija, new { htmlAttributes = new { @placeholder = "Naziv kategorije..." } })
                @Html.HiddenFor(model => model.Id, new { Value = ViewBag.Id })
                <button name="azurirajKat" type="submit">
                    Ažuriraj
                </button>
            }
        </section>
    </section>
</body>
</html>

@Styles.Render("/Content/style.css")
@Scripts.Render("~/bundles/jquery")
@Scripts.Render("~/bundles/jqueryval")